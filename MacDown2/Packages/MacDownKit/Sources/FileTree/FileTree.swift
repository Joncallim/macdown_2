/// FileTree — headless folder-tree model, filesystem seams, and bookmark-backed
/// shared preferences used by the app target's native sidebar.
public enum FileTree {
    public static let moduleName = "FileTree"
}
