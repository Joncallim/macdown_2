import Foundation

/// Module namespace marker; keeps the `MarkdownEngine.moduleName` test stable
/// after the EPIC-02 placeholder renderer was deleted.
public enum MarkdownEngine {
    public static let moduleName = "MarkdownEngine"
}
