import SwiftUI

/// Placeholder shell. Replaced by the real workspace shell in EPIC-02 (#3).
struct ContentView: View {
    var body: some View {
        Text("MacDown 2")
            .font(.largeTitle)
            .frame(minWidth: 640, minHeight: 480)
    }
}

#Preview {
    ContentView()
}
