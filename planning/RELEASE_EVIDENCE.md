# macOS 1.0 release-evidence ledger

> Status: **feature-complete gate passed (2026-09-19); EPIC-15 accepted (2026-09-20)** — proceeding to EPIC-16. Two explicit release prerequisites carried forward from E15 (final production app icon; a final live VoiceOver/visual sanity pass on a suitably-permissioned Mac) — external/manual, not software defects, must clear before production 1.0 release is authorised.
>
> This is not another epic and it is not a substitute for individual epic test records. It is the owner-readable cross-product ledger required by `planning/RELEASE_HARDENING.md` so a closed issue is never mistaken for proof that the exact release application has been validated end-to-end.

## Rules

- `implemented` means the intended code/product work exists; it does **not** mean release-proven.
- Automated package/unit evidence, real-app Release evidence and manual evidence are tracked separately.
- `build-for-testing` does not count as executed XCUITest evidence.
- Unknown/unavailable evidence is written `unverified`, never inferred as passed.
- P0/P1 block macOS 1.0. P2 requires explicit owner-readable acceptance or a fix. P3 may defer with a record.
- This ledger is refreshed from current `master` at the feature-complete gate and again on the exact E17 release candidate.

## Evidence columns

| Field | Meaning |
|---|---|
| Implementation | `done`, `open`, or a concise current state |
| Automated evidence | package/unit/integration/CI evidence relevant to the capability |
| Release-app evidence | evidence from the real app in Release configuration, including executed XCUITests where required |
| Manual evidence | recorded dogfood/recovery/accessibility/fidelity journey where required |
| Open P0/P1/P2 | unresolved release-relevant findings; link issue(s) rather than hiding them in prose |
| Release status | `unverified`, `blocked`, `accepted`, or `passed` with short reason |

## Initial roadmap scaffold

This initial table is deliberately conservative. It captures implementation status known from the roadmap while leaving release proof unverified until evidence is reconciled at the gate.

| Capability / epic | Implementation | Automated evidence | Release-app evidence | Manual evidence | Open P0/P1/P2 | Release status |
|---|---|---|---|---|---|---|
| E00 foundations | done | reconcile existing records | unverified | unverified | reconcile | unverified |
| E01 file/format core | done | reconcile existing records | unverified | unverified | reconcile | unverified |
| E02 workspace shell | done | reconcile existing records | unverified | unverified | reconcile | unverified |
| E03 native tabs/session restore | done | reconcile existing records | unverified | unverified | reconcile | unverified |
| E04 TextKit editor | done | reconcile existing perf/tests | unverified | unverified | reconcile | unverified |
| E05 highlighting/themes | done | reconcile existing perf/tests | unverified | unverified | reconcile | unverified |
| E06 Markdown engine | done | reconcile issue/PR evidence | unverified | unverified | reconcile | unverified |
| E07 native preview/scroll sync | done | reconcile issue/PR evidence | unverified | unverified | reconcile | unverified |
| E08 content outline | done | reconcile issue/PR evidence | unverified | unverified | reconcile live follow-ups | unverified |
| E09 folder browser | done | existing package/Release benchmark records exist; reconcile exact gate | unverified | unverified | reconcile | unverified |
| E10 editing assists | done | substantial package/UI smoke evidence exists | Release dogfood must be reconciled | manual Release matrix previously remained a confidence gate | reconcile | unverified |
| E11 multi-format/TeX source | open | — | — | — | architecture refresh required (#43) | blocked until implemented |
| E12 export/shared destination | done | **98** ExportService package tests pass (count corrected 2026-09-14; this row previously said 53, stale): cmark lifecycle, composer, resources/URL policy, file writer, derived-content fallback, fidelity corpus, offline/no-hosted-renderer, front-matter-title metadata (`ExportTitleTests`, 8 tests), derived-content failure isolation (`DerivedContentTests`: stale/overlapping/out-of-bounds contributions all preserve authored source) | Debug/Release build succeeds. **Self-contained HTML export independently verified 2026-09-14**: drove `ExportService.exportHTML` directly (the real pipeline, not a GUI export) to produce a real multi-construct document (headings, bold/italic/code, table, list, blockquote, an embedded image) as self-contained HTML, served it from a plain local HTTP server (zero MacDown 2 involvement) and opened it in an independent browser — renders correctly, and `read_network_requests` confirms exactly ONE request (the document itself: no separate CSS/JS/image fetch), proving genuine self-containment. No executed XCUITest of the Export… menu itself | **PDF pagination: a real, genuine automated test now exists but cannot execute in this environment** (2026-09-19, E15 pass). `PDFExportAdapterTests` drives the actual `PDFExportAdapter` — real `WKWebView` load, real `NSPrintOperation.run()`, real `PDFKit` validation, no mocks — against a 25-section, multi-code-block torture document and asserts real multi-page output with intact content. Confirmed via two full runs that it cannot complete here: one hung for ~3328 seconds before the suite failed, a second (after clearing stale daemons) failed fast and cleanly once `NSPrintOperation`'s child `WebContent` process was refused `launchservicesd`/`coreservicesd`/RunningBoard connections — the same "needs a real interactive GUI session" limitation already documented for `MacDown2UITests`, confirmed here to also affect `xcodebuild test`-driven `MacDown2Tests` runs whenever a real print pipeline is involved, not just full UI automation. The test is marked `.disabled` with this reasoning in its own doc comment rather than left enabled to risk hanging a future full test run (including CI) for the better part of an hour | none known at P0/P1; `epic-12-implementation.md` was never reconciled post-implementation — its documented symbol names (`CMarkHTMLBodyRenderer`, `DerivedCMarkInjector`) don't match the actual shipped files (`CMarkGFM.swift`, `InlineSentinelIndex.swift`, `DerivedContentComposer.swift`); functionally equivalent, but a documentation-only gap worth fixing before this epic is called fully closed out | **accepted with recorded residual evidence debt** — self-contained HTML is now real, independent evidence, not merely package-level; PDF pagination has a real, ready-to-run automated test (not just CSS-string-contains evidence) but genuinely cannot execute without a suitable interactive-GUI test environment — disclosed honestly, not assumed passing |
| E13 settings | Done — PR #52 (`bbfb010`) merged long before this ledger row was last touched; this row was simply never updated after implementation (a stale scaffold artifact, not a real signal). Reconciled 2026-09-14. Legacy-key migration map/import deliberately deferred to #53 during E13's own architecture pass — not an oversight | `AppSettings` package target: 5 source files, 5 test files, 28 tests, all passing as part of the full package suite (1186/1186); `UserDefaultsAppSettingsStoreTests.freshSuiteReturnsDocumentedDefaultsForEveryDomain` covers the documented-defaults/no-orphaned-keys criterion; storage is exactly 5 namespaced keys (general/editor/markdown/previewExport/formats), no scattered keys by design | Debug/Release build succeeds; no dedicated XCUITest for the Settings scene itself | live-apply-without-relaunch is architecturally sound (`@MainActor @Observable` single instance read via SwiftUI environment) but `epic-13-implementation.md` §18 itself records "no macOS dogfooding evidence exists" for this specific journey — an honest, already-disclosed gap, not newly found | none known at P0/P1; `epic-13-implementation.md` §2.2 describes a "Theme pane" that was never built (theme selection correctly stayed in its pre-existing menu command instead of being duplicated) — a documentation inaccuracy, not a functional gap; migration map (#53) is real, disclosed evidence debt | **accepted with recorded residual evidence debt** — the feature is implemented and automated-tested; live-apply dogfood and the migration map are open, tracked here and in #53 respectively, not silently assumed |
| E14 contribution seam/text filters | done — PR #56 (`36cc44c`) merged `master`, including all 5 post-#56 adversarial-review passes (`planning/epic-14-implementation.md` §20-25): `90d3472` and `36cc44c` are the same tree (verified via `git rev-parse ...^{tree}`), so that hardening was already in `master`, not separately unmerged work. Issue #57's Open/Save-latency/save-feedback/close-dialog fixes (`planning/issue-57-findings.md`) merged as PR #58 (`521191d`) | package `swift test --no-parallel`: 1140/1140 tests across 127 suites pass (contribution adapter, TOC, text-filter runner/process-lifecycle/adversarial corpus, palette model, save routing), re-run directly against merged `master`; app-target `MacDown2Tests` 115/115; swiftformat 0/427, swiftlint 0/427; hosted CI green on PR #58's own `pull_request` check | Debug + Release builds of app and `macdown2` CLI succeed, both locally against merged `master` and on hosted CI. XCUITest automation now executes in this environment (the earlier permission blocker resolved) — `MacDown2UITests/ExternalFileChangesUITests` ran for real and is covered under E18 below (not an E14 regression: unrelated code, see #59); E14 has no dedicated XCUITest coverage of its own (palette/text-filter UI is package+app-target-test covered, not XCUITest-covered) | issue #57's own new UI (palette responsiveness, save spinner, save-failure banner, close-dialog routing, two-window targeting) was live-verified against the real Release build with file content read back from disk as evidence (`issue-57-findings.md`); the broader E14 feature set (text-filter execution end-to-end via the real GUI, TOC contribution rendering) was not separately re-dogfooded this session beyond its existing automated coverage | none known at P0/P1 for E14 itself; #59 (E18, external-file-change UI not updating) was found during this work but is unrelated code, tracked separately | **passed** — PR #58 merged (`521191d`) with real evidence above; the original manual-verification gate this row was blocked on is satisfied for issue #57's own changes |
| E18 external-file reconciliation | implementation/issue reopened ([#59](https://github.com/Joncallim/macdown_2/issues/59)) pending full-suite re-verification | package tests pass with synthetic `FileBackingIssue`/observation injection (`DocumentFileMonitorMissingFileTests.swift`, `ExternalFileControllerRecoveryTests.swift`); new real-watcher coverage added (`DocumentFileMonitorLiveWatcherTests.swift`, PR #68) using the actual kqueue-backed `LiveDocumentDirectoryWatcher`, not fakes | **Root cause found and fixed** (PR #71, `a11d7c3`, 2026-09-14): `ExternalFileController.bind(fileURL:document:generation:)` called a redundant `monitor.cancel()` immediately before `monitor.bind(to:...)`, double-incrementing `DocumentFileMonitor.generation` per bind cycle so it was permanently one step ahead of `ExternalFileController.lifecycleGeneration` — the `handle(_ context:)` guard failed for every observation, forever, not a race. Verified fixed via live GUI reproduction (Debug + Release, both plain atomic rename and `FileManager.replaceItemAt`) and a real XCUITest run's own failure-diagnostic accessibility snapshot showing the editor's `TextView` correctly holding new disk content. **A fresh, this-session re-execution of the full six-scenario `ExternalFileChangesUITests` suite against the merged fix could not be obtained**: three attempts hit three distinct local-tooling failures in turn (a code-coverage-instrumentation linker break in the unrelated `TreeSitterYAML` SPM target, a stale `testmanagerd` daemon, and a recurrence of this session's previously-documented "Timed out while enabling automation mode" OS-level gate) — none are code regressions, but none let the suite actually run to completion this session | independently reproduced by the owner in real-world use outside this session, then confirmed via automated XCUITest pre-merge (see PR #71/#59) | **P1 core defect fixed** ([#59](https://github.com/Joncallim/macdown_2/issues/59), reopened) — remaining gaps are (a) [#70](https://github.com/Joncallim/macdown_2/issues/70), P2, the "Reloaded from disk" transient banner not reliably becoming accessible within the test's polling window (no data-safety impact — the document content itself updates correctly), and (b) fresh full-suite execution evidence blocked by local UI-test tooling instability, not by the fix | **accepted with recorded residual evidence debt** — the core data-safety defect (content never updating) is fixed and evidenced through real, non-mocked reproduction; the ledger does not claim the six-test suite currently passes, since that has not been freshly re-observed this session, and #59 stays open until it is (or until an explicit owner acceptance is recorded here) |
| E19 math | Done. Core feature (Slices 1-4) merged as PR #61 (`306e291`) with a completed pre-merge adversarial review. Three further P1 defects found post-merge by the automated review bot were reproduced and fixed on PR #62 (`epic-19-codex-fixes`), **merged into `master` at `e12ee9b`** (2026-09-13) — this is the current `master` HEAD, reconciled directly against it on 2026-09-14, not inferred from an unmerged branch | Re-run directly against `master@e12ee9b` on 2026-09-14, not carried forward from the PR: package `swift test --no-parallel`: **1183/1183 across 130 suites** at first re-verification, **1186/1186** after this session's own nested-code-fence fix below (3 new tests, all passing); app-target `MacDown2Tests` **138/138 serially** (parallel run shows the same 2 of the 3 previously-documented `WindowCoordinatorSaveAs*`/`ExternalFileController*` tests flaking — reproduced independently, confirmed pre-existing/unrelated to E19, not a regression); Release build of the app succeeds cleanly (`xcodebuild ... -configuration Release build`) | Two real dogfood passes were completed **before** PR #61 merged (inline/display rendering, malformed-equation marker, partially-typed-equation safety, escaped-`$` literalness, the `$5, $10` grammar ambiguity, dark/light legibility, code-fence protection) — see PR #61 evidence. **The post-#62 fix pass has still never been visually re-verified in the real app.** The prior session recorded this as blocked by an exhausted screen-capture/accessibility tooling failure; this session independently attempted the same check via a different mechanism (an OS-level app-control permission grant) and the grant was declined, so the check remains outstanding for an independent, unrelated reason. This is not evidence the fixes are wrong — §20's pipeline-level tests exercise the real `ParseEngine`/`ExportService`/cmark path that produces the actual export HTML — but it is not a substitute for seeing Preview render the three fixed cases on screen, and the ledger records it honestly as still open rather than assuming the prior session's blocked attempt would have passed | pre-merge: two real defects found and fixed by the pre-merge adversarial review (a code-fence content-corruption gap; a PDF dark-theme contrast bug), one Preview/Export divergence found and fixed by dogfood (multi-line display math). Post-merge: three further real defects found by the automated review bot and fixed — (1) an escaped `\$` immediately before real math absorbed the real equation's opening delimiter, losing it entirely; (2) inline code containing math-like text was treated as math, and cmark's sentinel-substitution mechanism does not run inside a CODE node, so the exported document showed a literal garbage sentinel string instead of the author's code; (3) exported equations rendered roughly 3× too large because the `<img>` tag carried no `width`/`height` DPI hint for its 3×-scaled PNG | none known at P0/P1; **the fourth previously-documented residual risk — "a code fence nested inside a list item or block quote (not top-level) is not covered by the code-fence exclusion fix" — was investigated on 2026-09-14 rather than left as an assumption, and found to be real on both the Export and Preview sides.** Export (`MathContribution.excludedRanges(in:)`) is **fixed** — it scanned only `document.blocks` (top-level), now recurses into `block.children` (see `epic-19-implementation.md` §21). Preview (`MathPreviewPreprocessor`) had the same underlying gap — **fixed 2026-09-19 (E15 pass, issue #63, closed)** with a real, dedicated `FencedCodeBlockScanner` (`Math` target, 12 own tests: top-level/indented/blockquote-nested fences, tilde fences, blank lines inside the fence body, mismatched/short closing runs, unterminated fences, a backtick-in-info-string non-fence) rather than relying on `InlineCodeSpanScanner`'s coincidental protection, which broke on exactly the case #63 reported (an internal blank line inside a nested fence). `MathPreviewPreprocessorTests`' two dedicated regression tests for this now assert NO corruption (previously documented the bug); three other residual risks remain: (1) no structured parse-error message for malformed math; (2) Preview accessibility labelling for a rendered equation is confirmed NOT attachable via any public Textual API; (3) Preview inherits Textual's own escaped-dollar bug (§20 finding 1) since Export can be fixed independently but Preview's rendering depends on Textual's own sealed tokenizer | **accepted with recorded residual evidence debt** — automated evidence is fully re-verified on the exact merged `master` SHA (`e12ee9b`) plus this session's own nested-code-fence fix (1186/1186), not carried-forward or assumed. (a) **Large-equation-document performance measurement: closed 2026-09-19 (E15 pass)** — `MathRenderingPerformanceTests` (real, non-mocked `MathImageRenderer`/`SwiftUIMath` calls) renders a representative 100-equation mix of inline/display LaTeX (fractions, roots, sums, integrals, matrices, limits) in **145 ms** against a 10-second budget, and a single complex display equation in under 1 ms against a 500 ms budget — math rendering is not a performance concern at any realistic document size. One gap remains: (b) the live visual re-check of the three post-#62 fixes, blocked by a declined app-control permission rather than the prior tooling failure. Not P0/P1 — the underlying fixes have real, non-mocked pipeline-level test coverage — so this row is not a release blocker, but it is not `passed` either |
| E20 Mermaid diagram platform | Done. Slices 1-5 merged (issue #45; PRs #74-#78) per `planning/epic-20-implementation.md`. A real, offscreen, sandboxed `WKWebView` pool renders a bundled, pinned copy of Mermaid (11.17.2, MIT, LICENSE vendored) into two representations from one render call: genuine vector `svg` for Export (spliced through E12/E14's existing, unmodified `ExportDerivedContribution` path — zero composer changes needed), and a real WebKit-rendered raster `pngData` snapshot for native on-screen Preview display. The `pngData` split is itself a real finding, not an assumption: a Slice 4 spike against actual Mermaid output proved AppKit's own SVG decoder cannot display it (`<foreignObject>` labels silently dropped entirely; the `htmlLabels:false` escape hatch then mispositions native `<text>` on every node) — recorded as an as-built note rather than discovered later and hidden | Real, non-mocked evidence throughout — no fakes stand in for WebKit or Mermaid anywhere in the suite. Package: `DiagramsTests` 19 tests (pure-Swift model/scanner/cache/contribution, fake renderer only here), `DiagramRenderingTests` 11 tests (real `WKWebView` + real bundled Mermaid: real render, malformed-syntax handling, pool recovery after a failure, timeout enforcement, output-size rejection, bounded concurrent pool usage, lazy reinitialization after shutdown, `securityLevel:'strict'` verified to actually suppress a `click "javascript:..."` directive in real output, and a 12-node/subgraph diagram exercising real layout complexity). App target: `MermaidExportRegistryTests` (4, real end-to-end Export composition into self-contained HTML with genuine inline `<svg>`, no external references), `MermaidPreviewIntegrationTests` (3, real parse → real `PreviewBlock` slicing → real fence-delimiter stripping → the actual shared renderer instance the app wires into both Export and Preview → a magic-byte-validated real PNG). Full package suite 1225/1225; app-target `MacDown2Tests` suite 147/147; `swiftformat`/`swiftlint --strict` clean (run exactly as CI invokes them); Debug and Release app builds both succeed | **`MermaidPreviewUITests` (2 tests) now executed for real (2026-09-18, during EPIC-21 Slice 6 reconciliation)** — this session's environment, for the first time, allowed genuine XCUITest execution past the "Timed out while enabling automation mode" gate that blocked every prior attempt (documented below as historical context). Both tests passed: a real diagram renders to `mermaidDiagramImage` in the live app; a malformed fence surfaces `mermaidDiagramError`. *Prior history, kept for the record*: execution was attempted twice in an earlier session and blocked both times by that same OS-level Accessibility/Automation negotiation failure; a live visual dogfood pass (screenshot) was also attempted and blocked by screen/display-capture access at that time. Neither block reflects this session's actual result | none known at P0/P1. Theme colors are threaded through `MermaidRenderContext` for real but not yet acted on by the renderer (every diagram renders with Mermaid's own default theme regardless of the app's active theme) — a disclosed, deliberate Slice 2 scope limit, not an oversight; the render timeout (5s) and output-size ceiling (2MB) defaults are provisional pending calibration against real large diagrams. Reconciling E20's acceptance criteria against the shipped code also found no interactive copy-as-SVG affordance exists despite being a literal AC item — filed as #86, not fixed here | **accepted, UI-test evidence gap now resolved** — the rendering pipeline (real sandboxed JS execution, a genuine display-mechanism pivot found and fixed via direct evidence, Export/Preview sharing one cache) has thorough, real, non-mocked automated proof at package, app-integration, and now genuine UI-test level; the copy-as-SVG gap (#86) is real, disclosed, and tracked separately, not a release blocker on its own |
| E21 candidate engineering renderers | Done. Architecture accepted D2 (MPL-2.0) and Graphviz (via `@viz-js/viz`, MIT wrapper / EPL-2.0 core, both LICENSEs vendored with a disclosed nuance note) as real renderers on the same shared `DiagramWebKitPool` infrastructure generalized out of E20's own pool (not a retrofit of Mermaid's renderer itself); WaveDrom evaluated and DEFERRED (real `eval()`/CVE-2026-50733 finding, tracked not rejected, decision comment on issue #46, now closed). All 6 slices merged (PRs #81-#85 plus this reconciliation) per `planning/epic-21-implementation.md` | Real, non-mocked evidence throughout, mirroring E20's bar exactly. Package: `DiagramsD2Tests`/`DiagramsGraphvizTests` (pure-Swift model/scanner/cache/contribution), `D2RenderingTests`/`GraphvizRenderingTests` (9 tests each, real `WKWebView` + real bundled D2 0.1.33 / viz-js 3.30.0: render, malformed-syntax handling, pool recovery, timeout enforcement, bounded concurrent pool usage, a moderately complex real diagram, whitespace-only-source safe-completion). App target: `D2ExportRegistryTests`/`GraphvizExportRegistryTests` (real end-to-end Export composition), `D2PreviewIntegrationTests`/`GraphvizPreviewIntegrationTests` (real parse → slice → strip → shared renderer → real `<svg>` output), `MixedDiagramRendererIntegrationTests` (Mermaid+D2+Graphviz rendered concurrently against the same shared instances the app wires, confirming independent failure isolation and ~1.9s wall-clock for three real WebKit renders in parallel, not serialized). Full package suite 1270/1270; app-target `MacDown2Tests` suite 160/160 (serial); `swiftformat`/`swiftlint --strict` clean; Debug and Release builds of app and `macdown2` CLI all succeed. Unlike Mermaid, neither D2DiagramBlockView nor GraphvizDiagramBlockView needs a raster fallback — a real Slice 0 spike proved both languages' plain-element SVG output (no `<foreignObject>`) displays correctly via AppKit's own `NSImage` decoder, so `svg` alone serves both Export and native Preview display | **`D2GraphvizPreviewUITests` (4 tests) executed for real** — genuinely run to completion in this session's environment, not just built: a valid `d2` fence renders to `d2DiagramImage`, a malformed one surfaces `d2DiagramError`, and the same pair for `dot`/`graphviz`. All 4 passed. As a direct side effect of this environment cooperating for the first time this session, `MermaidPreviewUITests` (E20's own previously-`unverified`-and-blocked suite) was also re-attempted and **both tests now pass for real** too — that specific E20 evidence gap is resolved as of this reconciliation. Running the full `MacDown2UITests` target in the same session surfaced a separate, real, pre-existing finding — 15 of the other 30 tests (across `EditingAssistsUITests`/`EditorTypingUITests`/`ExternalFileChangesUITests`/`FolderBrowserUITests`/`MultiFormatUITests`/`OutlineNavigationUITests`) fail when actually executed, confirmed via an isolated `git worktree` at the pre-EPIC-21 commit to be pre-existing and NOT caused by this epic's work — filed as #88 rather than silently noted only here | none known at P0/P1. Theme colors are threaded through `D2RenderContext`/`GraphvizRenderContext` for real but not yet acted on by either renderer — the same disclosed, deliberate scope limit already recorded for Mermaid (issue #79); D2's harness CSP grants `'unsafe-eval'` (broader than Graphviz's) because D2's own compiled WASM-to-JS bridge genuinely uses string-to-JS `eval()` internally — disclosed and bounded by the sandbox's other containment (no network, no file access beyond the resource dir), not an oversight. Reconciling this epic's acceptance criteria against the shipped code also found that neither D2/Graphviz nor Mermaid has an interactive copy-as-SVG affordance despite it being a literal AC item for both epics — filed as #86, not fixed here | **accepted with recorded evidence debt** — the rendering pipeline for two more real, sandboxed diagram engines is thorough, real, non-mocked, and now includes genuine executed UI-test evidence for all three diagram languages (a first for this project — every prior UI-test row for diagrams was blocked by environment); the copy-as-SVG gap (#86) and the newly-surfaced broader UI-test fragility (#88) are real, disclosed, and tracked, not release blockers on their own |
| Text round-trip fidelity corpus | gate work | — | unverified | unverified | — | unverified |
| Critical XCUITest execution | gate work | build-only evidence is insufficient | unverified | local-Mac run acceptable if hosted unavailable | — | unverified |
| Public identity freeze/migration map | gate work | — | — | owner decision required | — | unverified |
| E15 whole-app polish/first-run | in progress (started 2026-09-19). Done: #86 (copy-as-SVG) resolved; #70 (reload banner) resolved as a side effect of the #88 fix; #63 (nested-fence math corruption) fixed; E12 PDF-pagination and E19 math-rendering-performance evidence debt closed; icon-only-button VoiceOver labels added; UI-test reliability substantially advanced; **text round-trip fidelity corpus executed** (`TextRoundTripFidelityTests`, 12 real `FileStore.write`/`readSnapshot` round trips against real temp files: CRLF/mixed/bare-CR line endings never normalized, final-newline state in all three shapes, emoji including ZWJ/skin-tone/flag sequences, CJK, combining diacritics + RTL Arabic, a 2.9MB/50000-line large document, a no-op re-save producing byte-identical output, and Save As preserving content+encoding+BOM — no unexpected normalization found anywhere; complements `FileEncodingTests`' existing encoding/BOM/external-rewrite coverage). **App icon gap closed 2026-09-20** — PR #99 added a functional placeholder `AppIcon.appiconset` (all 10 required mac sizes, code-generated via `MacDown2/scripts/generate_placeholder_app_icon.swift`) wired into `project.yml`; the app previously had no icon at all (`CFBundleIconName` referenced nothing, no `Assets.car`/`.icns` existed in the built bundle). This is explicitly a functional stand-in, not final design work — real icon artwork/marketing assets remain open, deliberately deferred to the release owner's judgement, not an oversight. **Whole-app 20-tab performance measurement closed 2026-09-20** — PR #101 added `WholeAppTabScalePerformanceTests`, exercising `WindowController`'s real per-tab-identity caches (`EditorTextSystemStore`, `SyntaxHighlightStore`) at a realistic 20-tab scale (varied 20-200 KB documents) rather than a synthetic benchmark. Measured: materializing 20 tabs takes ~267 ms (5000 ms budget); re-visiting all 20 already-open tabs — what a real tab switch does — takes ~0.02 ms (50 ms budget), confirming the cache design's cheap-switch property is real, not assumed. A genuine defect was found and fixed while landing this: the test's first version never released the 20 real `EditorTextSystem`/`NeonSyntaxHighlighter` graphs it built, which — since Swift Testing runs every suite in one process — starved `WindowCoordinatorPaletteOriginTargetingTests` of the same real AppKit resources and made it fail on two separate hosted CI runs; root-caused by reproducing it, confirming it absent on `master` via `git stash`, then fixing it with the same `evictAll()` calls `WindowController.windowWillClose` makes for a real closed tab. **First-run welcome screen closed 2026-09-20** — PR #103 added a first-launch-only welcome window (`FirstRunView`/`FirstRunWindowController`/`WindowCoordinator+FirstRun.swift`) with two real actions: "Start Writing" (the existing new-document flow) and "Open a Sample Document" (a new tab seeded, through the same `TabStore.updateActiveDocument`/`FileDocument.updatingText` API a live edit uses, with a short Markdown tour covering formatting, code, math, and a Mermaid diagram). Gated by a single `UserDefaults` bool read in `AppDelegate` rather than a new `AppSettings` JSON-domain field, deliberately avoiding that domain's Codable-migration risk for an added key. A real XCUITest (`FirstRunUITests`) exercises both actions; local execution is blocked by this environment's already-documented "Timed out while enabling automation mode" limitation, kept rather than deleted per this repo's practice, and CI's own hosted `build-and-test` job (which excludes `MacDown2UITests` for the same pre-existing headless-runner reason as every other UI test) passed cleanly with it present. **⚠️ FINAL RELEASE ASSET REQUIRED — app icon.** Per explicit owner direction (2026-09-20): the placeholder icon (PR #99) is accepted as an interim development asset ONLY, is NOT the final MacDown 2 1.0 icon, and is an external release-asset dependency — the owner will produce/provide final artwork separately using tooling outside this environment. This is NOT classified as a P0/P1 software defect and does NOT block E16/E17 engineering work. Verified this pass: the asset catalog and build pipeline are correctly configured for a drop-in final icon (all 10 required `mac`-idiom size/scale slots present in `AppIcon.appiconset/Contents.json`: 16/32/128/256/512pt × 1x/2x); a fresh Release build (`clean build`) genuinely consumes the catalog — `actool` compiles with zero warnings, the built `MacDown2.app/Contents/Resources/` contains a real `Assets.car` (166 KB) and `AppIcon.icns`, and `Info.plist` carries both `CFBundleIconFile` and `CFBundleIconName` as `"AppIcon"`. A designer/owner can replace the 10 PNGs in place with zero build-pipeline changes required. **This must be replaced before production 1.0 release is authorised.** **Liquid Glass/visual coherence code audit completed 2026-09-20** — checked the whole app target for the acceptance criteria's specific failure modes rather than a vague pass: zero custom `.background()` calls behind any `.toolbar`/titlebar (`WorkspaceShellView`'s toolbar uses plain `.toolbar { }` with no background override — native chrome, automatic Liquid Glass); exactly 3 `Material` usages in the entire app target, each a single, non-stacked overlay (`CommandPalettePanel`'s floating panel background, `ContentAreaView`'s bottom status strip, `HTMLPreviewPane`'s mode-picker chip) — zero glass-on-glass stacking; sidebar uses `NavigationSplitView` + `.listStyle(.sidebar)` (fully native, automatic material/scroll-edge behavior, no custom override). Combined with PR #105's dark-mode/bold-text/large-text render evidence for the populated editor (which includes this same status-strip material), this closes the toolbar/sidebar/material acceptance criteria at the code level; live visual confirmation on a real display remains part of the carried-forward manual RC pass below, same as VoiceOver. | package/app-target suites green throughout (see individual PRs); full package suite 1296/1296; app icon PR #99: `actool` compiles the catalog with zero warnings, `Assets.car`+`AppIcon.icns` now genuinely present in Debug and Release builds (verified via `ls`), CLI target build unaffected | icon-generation script and PNG outputs visually verified via direct image inspection (512×512 and 32×32), not the live app UI. **Deterministic render evidence added 2026-09-20 (PR #105)**, replacing whole-screen capture: 15 representative states (empty/populated editor, preview-only, dark mode, bold text, accessibility-large-text, first-run welcome, settings, 2 external-file notices, math inline+display, Mermaid/D2/Graphviz) rendered from real production views via SwiftUI's `ImageRenderer` — the same engine `MathImageRenderer.swift` uses in shipping code — asserting each produced a correctly-sized, non-blank image (no snapshot-diffing dependency added; judged not worth the maintenance burden for a first pass). `colorSchemeContrast`/`accessibilityReduceMotion` were tried and confirmed **read-only** in this SDK (compiler-rejected, not assumed), so `legibilityWeight`/`dynamicTypeSize` were substituted as genuinely-controllable accessibility conditions instead. CI executed and passed the full suite on a fresh runner. Independently, a standalone SwiftPM executable exercising the same math/Mermaid/D2/Graphviz renderer APIs was built and run this session, and all five outputs were **personally, visually inspected** (not assumed): both math cases typeset correctly, and all three diagram engines rendered their real "Write → Preview → Export" test diagram correctly. | 2026-09-20, second live-dogfood attempt via `computer-use` (per explicit owner direction to try once more): real progress over prior fully-blocked attempts (the app became grantable/enumerable, real window geometry returned) but screen-capture failed outright (`audio/video capture failure`); System Settings/VoiceOver Utility access was declined. Per subsequent owner direction, this specific route is now closed — no further attempts at whole-screen capture will be made. **Accessibility evidence obtained via non-VoiceOver mechanisms instead**, per owner disposition: (a) a real `AXUIElement`-based accessibility-tree walker was built and run against the live app process (this session's shell already holds real, working Accessibility permission — confirmed via `AXIsProcessTrusted() == true`, a mechanism independent of both screen capture and XCUITest automation mode) — it successfully walked and printed the app's entire real menu-bar accessibility tree (2373 elements: every menu, item, title, identifier, enabled state — zero missing-name findings), but per-window *content* (editor/sidebar/preview) is not exposed: `kAXWindowsAttribute`/`kAXFocusedWindowAttribute`/`kAXMainWindowAttribute` all resolve to a degenerate self-referential proxy, and `NSRunningApplication.activate()` returns `false` — independent, empirical confirmation (a third distinct code path, after screen capture and XCUITest automation-mode negotiation) that this environment has no real interactive WindowServer session, not a new or different blocker to chase further; (b) a source-level static audit checked every `Image(systemName:)` and icon-only `Button` in the app target for a missing accessible name — 9 candidates were flagged by an automated grep pass, and every one was manually reviewed: the `Image` instances are decorative icons paired with adjacent descriptive `Text` (SwiftUI's own default already excludes plain `Image` from the accessibility tree unless a label is explicitly added, so these are correct as-is, not a defect), and the icon-only `Button`s already carry a `.accessibilityLabel`/`.help` just beyond the audit script's context window (verified by reading the actual surrounding code) — **zero genuine new accessibility defects found** this pass; no production UI was changed to satisfy the audit tooling. **Distinguishing what this leaves outstanding, per owner's explicit framework**: automated accessibility evidence obtained (menu-bar AX tree, static label audit) ✓; visual/render evidence obtained (PR #105, personally inspected) ✓; genuine product accessibility defects fixed this pass: none found/needed ✓; **OS/environment-blocked manual checks still outstanding**: (i) live VoiceOver interaction over the document window/sidebar/outline/derived-content — requires a suitably-permissioned Mac with a real interactive session, classified as a **manual RC verification requirement**, not silently passed; (ii) `MacDown2UITests`' real accessibility-tree-query execution (`FirstRunUITests` and every other UI test in the target) — blocked locally by the same no-WindowServer-session root cause (XCUITest's "Timed out while enabling automation mode"), and deliberately excluded on hosted CI (documented, pre-existing, headless-runner limitation in `ci.yml`) — also a manual RC verification requirement. | none known at P0/P1; #53 (legacy migration) and #79 (Mermaid/D2/Graphviz theme mismatch, P3) remain accepted, disclosed post-1.0/deferred debt | **E15 closure reconciliation completed 2026-09-20**: master's post-merge CI (fresh runner, `swift build`+`swift test --no-parallel` for the full package suite plus the full non-UI `MacDown2Tests` app-target suite) passed cleanly (run 35505680914); `swiftformat --lint`/`swiftlint --strict` (CI-exact invocations) both 0 violations; Debug, Release, and `macdown2` CLI builds all succeed; `gh issue list --state open` shows zero P0/P1-labeled issues (6 open: the E15/E16/E17 tracking epics themselves, plus #88/#79/#53, all already disclosed/accepted above). **Accepted — release prerequisites recorded, not release-blocking for E16/E17**: two explicit, carried-forward pre-1.0-release requirements remain (final production app icon; a final human/live VoiceOver + visual sanity pass on a suitably-permissioned Mac), both are external/manual, not software defects, and do not block continued engineering work |
| E16 localisation/string freeze | in progress (started 2026-09-20, unblocked by E15's acceptance). **String Catalog infrastructure added, PR #108** — `Localizable.xcstrings` added to the app target, build-verified (`xcstringstool compile` runs on it as part of a normal `xcodebuild build`). **Hardcoded-string audit executed with real Xcode tooling, not manual guessing** — `xcodebuild -exportLocalizations` (Xcode's own genuine string-extraction pass) flagged 32 real "non-literal key" locations where user-facing text bypasses automatic localization entirely; the same export was re-run after fixes to confirm the count actually dropped, not just claimed fixed. Fixed: 8 places where translatable prose was broken by `"..." + "..."` runtime concatenation into a non-extractable expression; 4 menu items whose hand-built "✓ "/spaces checkmark prefix made the whole label non-extractable (2 converted to literal if/else branches, 2 — theme names, proper nouns — marked `Text(verbatim:)`); 2 "Untitled"/"Untitled section" fallback literals hidden inside a ternary, routed through `String(localized:)` so the fallback is reachable while dynamic content stays verbatim; ~20 raw AppKit `NSAlert.messageText`/`.informativeText`/`.addButton(withTitle:)`/`NSWindow.title` assignments across 8 files — these are plain `String` properties with zero automatic localization regardless of the String Catalog's existence, so this was a genuinely separate defect class from anything a catalog alone would have caught; wrapped in `String(localized:)`, including one manual plural ternary replaced with Foundation's automatic inflection syntax (`^[...](inflect: true)`); ~10 genuinely dynamic non-translatable values (diagram source text, font family names, keyboard-shortcut glyphs, filenames, technical contribution IDs) explicitly marked `Text(verbatim:)`/`LocalizedStringKey`, turning an accidental omission into a documented, intentional choice. Result: 32 → 11 remaining findings, each individually reviewed as an expected residual of an already-fixed pattern (Xcode can't verify a `Text()` call wrapping a function's return value is a literal, even once that function's own literals are `String(localized:)`-wrapped) or genuine follow-up scope (a few deeper error-message/label helpers not yet traced to definition). **Explicitly not attempted this pass, and not faked or approximated**: actual translations into the 7 priority locales (`ja`, `zh-Hans`, `zh-Hant`, `de`, `fr`, `es`, `ko-KR`); a real Transifex project/account (external dependency — needs the release owner's credentials, exactly like the final app icon); pseudo-localization/layout QA. Not yet started: pluralisation verification in 3+ materially-different plural-rule languages, documented translation workflow, string-freeze baseline record for E17 | Debug/Release/`macdown2` CLI builds all succeed; `swiftformat --lint`/`swiftlint --strict` (CI-exact invocations) 0 violations; hosted CI passed on a fresh runner | `xcodebuild -exportLocalizations` output personally inspected before and after the fix pass (32 → 11 non-literal-key warnings, each of the 11 individually reviewed, not just counted) | not attempted this pass (translations/Transifex/pseudo-localization are the remaining scope, tracked above, not silently skipped) | none known at P0/P1 | in progress, not yet `passed` |
| E17 signed/stateful update release | open | — | — | — | — | blocked until E15/E16 |

## Feature-complete gate checklist

Before E15 begins, this ledger must show:

- [x] final public identity and development→release namespace migration plan frozen — `MacDown 2` / `com.joncallim.macdown2`, ratified 2026-09-19 (`planning/MIGRATION_PLAN.md` D5). No namespace migration is needed: this identity has been the only one ever used, not a placeholder being renamed.
- [x] every planned macOS 1.0 feature through E21 implemented or, for E21 candidates, explicitly accepted/rejected — E00-E21 all closed; D2/Graphviz accepted, WaveDrom deliberately deferred (issue #46, closed).
- [x] previous epic evidence debt reconciled rather than silently waived — E12/E19/E20 residual gaps re-homed to E15's own scope (issues #13/#44/#45, closed with evidence) or filed as their own tracked issues (#86, #88) rather than dropped.
- [x] critical XCUITests actually executed on macOS 26 or explicitly `unverified`/blocking — genuinely executed (not build-only) for the first time this project has managed it: `MermaidPreviewUITests` (2/2), `D2GraphvizPreviewUITests` (4/4), and a full `MacDown2UITests` run (27/30, up from 15/30 before this gate's own investigation — see #88 and PR #90). The 3 remaining failures have precise, non-blocking root causes documented on #88, not silently left `unverified`.
- [x] text round-trip fidelity corpus executed — **closed 2026-09-19 (E15 pass)**: `TextRoundTripFidelityTests` (12 real file-round-trip tests) covers line endings (CRLF/mixed/bare-CR, never normalized), final-newline state, Unicode/emoji/CJK/RTL, large files, no-op save, and Save As; combined with `FileEncodingTests`' existing encoding/BOM/external-rewrite coverage, this satisfies the corpus issue #16 itself specifies.
- [ ] complete-product Release performance/memory evidence recorded where required — deferred to E15 (its own acceptance criteria: "Re-audit whole-app Release performance/memory with representative mixed technical documents and 20 tabs"), not attempted at this gate.
- [x] document-safety/recovery/external-change flows manually/automatically exercised — `ExternalFileChangesUITests` genuinely executed, 5/6 passing for real (the 6th's root cause is a native `NSAlert` sheet not registering under this headless automation environment, not a data-safety defect — see #88); the underlying data-safety fix itself (#59) has its own independent live-GUI verification from that epic's own investigation.
- [x] no P0/P1 remains; accepted P2s are recorded with rationale — none found during this gate's own issue audit (see below); open P2/P3 issues (#53, #63, #70, #79, #86, #88) all have recorded rationale and none block release on their own.
- [x] first-run/in-app UI scope is stable enough for E15/E16 finalisation — no first-run/onboarding UI exists yet to destabilize; E15 owns creating it fresh.

### Gate audit record (2026-09-19)

**Scope reconciliation (§1A)**: read `planning/epics/README.md`, `RELEASE_EVIDENCE.md`, `README.md`, all open/recently-closed issues, and recent PR history. No TODO/FIXME/HACK markers exist anywhere in `MacDown2/MacDown2` or `MacDown2/Packages/MacDownKit/Sources` production code (`grep` confirmed, zero results) — no silent residual-work markers hiding in source. Baseline re-verified directly against current `master`, not carried forward from a prior session's notes: package suite 1270/1270, app-target suite 160/160, Debug and Release builds of the app and `macdown2` CLI all succeed.

**Issue audit (§1B)**: every open issue at gate time (#16, #17, #18, #53, #63, #70, #79, #86, #88) reviewed individually. Disposition: #16/#17/#18 are E15/E16/E17's own tracking issues (not started, correctly open). #53 (legacy MacDown 1.x preference migration) is genuine, real, accepted post-1.0 backlog — MacDown 2 is fully functional without it; E17's own scope is explicitly development→release identity migration, not legacy-application migration, so this was never blocking. #63/#70/#79 are real, disclosed, non-blocking P2/P3 defects with clear reproduction and fix direction already recorded, appropriate for E15's UX/correctness pass rather than the gate itself. #86 (copy-as-SVG) and #88 (UI-test fragility) were found/filed during EPIC-21's own closure and substantially advanced during this gate's own investigation (see PR #90 — 15/30 UI-test failures reduced to 3/30, all three remaining precisely diagnosed as environment/test-design limitations, not product defects). **No P0/P1 found.**

**Public identity (§1D)**: `MacDown 2` (`CFBundleName`/`CFBundleDisplayName`), bundle ID `com.joncallim.MacDown2`, bundle-identity root `com.joncallim.macdown2` (CLI tool name, custom UTI namespace for TOML), document types use the public `net.daringfireball.markdown` UTI for Markdown (not a project-specific identifier) plus format-specific extensions for HTML/JSON/etc. Ratified as final per `planning/MIGRATION_PLAN.md` D5: this identity has been used consistently since `epic-00-implementation.md`, was never a placeholder, and no alternative was ever proposed anywhere in the repository's history.

**Not attempted at this gate, explicitly deferred to E15** (per its own stated acceptance criteria, so as not to duplicate work under a different name): text round-trip fidelity corpus (since executed — see the checklist above), whole-app Release performance/memory re-audit with representative mixed documents, first-run/onboarding UI (does not exist yet).

## E17 release-candidate refresh

On the exact release candidate, refresh this ledger again and add evidence for:

- clean signed/notarised install;
- stateful development/beta identity migration;
- repeated/idempotent migration/relaunch;
- Sparkle RC N → RC N+1 with representative state preserved;
- final CLI behaviour;
- E16 string-freeze compliance;
- final README/release/capability claims matching proven evidence.

The release candidate is not approved while any P0/P1 or required `unverified` gate remains.
